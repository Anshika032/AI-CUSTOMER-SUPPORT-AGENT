[project]
name = "ai-customer-support-agent"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
    "openai",
    "requests",
    "pydantic",
    "streamlit",
    "python-dotenv",
]
